/** Host face. The browser face owns the full-page console. */
export const name = 'dsh-harness-ui'

/** Keep the package on the Host roster so its declared client entry is served. */
export function apply() {}
